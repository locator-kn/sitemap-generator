{
  "name": "sitemap-generator",
  "version": "1.0.0",
  "description": "node module for generating a sitemap.xml for https://locator-app.com",
  "main": "index.js",
  "repository": "https://github.com/locator-kn/sitemap-generator.git",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cradle": "^0.6.9",
    "sitemap": "^1.0.0"
  }
}
